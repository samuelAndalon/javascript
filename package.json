{
  "name": "javascript",
  "version": "1.0.0",
  "scripts": {
    "test": "jest --watchAll",
    "test-browser": "node --inspect-brk node_modules/.bin/jest --runInBand"
  },
  "devDependencies": {
    "jest": "^24.1"
  },
  "dependencies": {},
  "jest": {
    "testEnvironment": "node"
  }
}
